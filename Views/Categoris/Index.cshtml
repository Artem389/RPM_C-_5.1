@model IEnumerable<WebApplication5.Models.Categori>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameCategori)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameCategori)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCategori">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCategori">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCategori">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
