@model IEnumerable<WebApplication5.Models.Pasport>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Serial)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Number)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateOfIssue)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Serial)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Number)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateOfIssue)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPasports">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPasports">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPasports">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
